package me.connor;

import javax.swing.*;

public class AppFrame extends JFrame {

    // Constructors are like methods, but they have the exact name of the class and do not return anything, even void.
    // They are used to initialize the class with any required information provided by usage.
    // You can also do any "setting-up"

    // There is no type parameter necessary for the constructor
    public AppFrame(){

        // You don't need to put anything in here, but you might want to set the window dimensions so that it isn't smushed...
        setSize(600, 600);

    }
}
